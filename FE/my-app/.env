# API URL: use `.env.development` (local) and `.env.production` (deployed build).
# Do not point production at localhost — browsers block public HTTPS sites from calling loopback.

# Other configurations
REACT_APP_NAME=Movie Ticket Sales

# Disable source maps to avoid warnings
GENERATE_SOURCEMAP=false
